import React, { Component } from 'react';

export default class SassOnly extends Component {
  render () {
    return (
      <div className="sass-only">
        <h1>This component is styled with Sass</h1>
      </div>
    );
  }
}
